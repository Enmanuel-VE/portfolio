<div class="icon-linter">💡</div>

<style>
  .icon-linter {
    width: fit-content;
    position: relative;
    display: flex;
    animation: animation-rotate 800ms alternate infinite;
  }

  @keyframes animation-rotate {
    from {
      transform: rotate(15deg);
    }
    50% {
      transform: translateY(-4px);
    }
    to {
      transform: rotate(-15deg);
    }
  }

  .icon-linter::after {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 100%;
    aspect-ratio: 1/1;
    border-radius: 100%;
    z-index: 0;
    background-color: rgba(255, 255, 0, 0.25);

    animation: animation-pulse 700ms infinite alternate;
  }
</style>
